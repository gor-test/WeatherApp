import React from 'react';
import { ScrollView, Text, View } from 'react-native';

const WeatherView = () => (
  <ScrollView
    contentInsetAdjustmentBehavior="automatic"
  >
    <View>
      <Text>Hello Marko!!!</Text>
    </View>
  </ScrollView>

);

export default WeatherView;
